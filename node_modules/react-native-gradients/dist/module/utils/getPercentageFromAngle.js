const step = 100 / 90;
export const getPercentageFromAngle = function (angle, minVal) {
  let isRev = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : false;
  const actualDeg = angle - minVal;
  const percentage = step * actualDeg;
  return isRev ? 100 - percentage : percentage;
};
//# sourceMappingURL=getPercentageFromAngle.js.map